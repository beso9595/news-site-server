# News Site server side

using: `express` and `mongoose`\
client side: [github.com/beso9595/news-site-client](https://github.com/beso9595/news-site-client)

* `git clone`
* `npm i`
* fill `.env`
* `npm start`
* deployed to `localhost:{yourPort || 3000}`